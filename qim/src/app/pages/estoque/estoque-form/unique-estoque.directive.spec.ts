import { UniqueEstoqueDirective } from './unique-estoque.directive';

describe('UniqueEstoqueDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueEstoqueDirective();
    expect(directive).toBeTruthy();
  });
});
