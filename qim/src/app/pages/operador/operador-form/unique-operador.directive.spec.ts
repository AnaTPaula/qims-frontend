import { UniqueOperadorDirective } from './unique-operador.directive';

describe('UniqueoOperadorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueOperadorDirective();
    expect(directive).toBeTruthy(); 
  });
});
