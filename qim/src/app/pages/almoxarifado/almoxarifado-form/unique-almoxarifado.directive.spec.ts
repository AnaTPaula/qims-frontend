import { UniqueAlmoxarifadoDirective } from './unique-almoxarifado.directive';

describe('UniqueAlmoxarifadoDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueAlmoxarifadoDirective();
    expect(directive).toBeTruthy();
  });
});
