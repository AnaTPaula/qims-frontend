import { UniqueFuncionarioDirective } from './unique-funcionario.directive';

describe('UniqueFuncionarioDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueFuncionarioDirective();
    expect(directive).toBeTruthy();
  });
});
