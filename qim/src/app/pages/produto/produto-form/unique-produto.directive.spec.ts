import { UniqueProdutoDirective } from './unique-produto.directive';

describe('UniqueProdutoDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueProdutoDirective();
    expect(directive).toBeTruthy();
  });
});
