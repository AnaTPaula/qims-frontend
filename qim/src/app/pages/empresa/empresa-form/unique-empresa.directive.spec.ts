import { UniqueEmpresaDirective } from './unique-empresa.directive';

describe('UniqueEmpresaDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueEmpresaDirective();
    expect(directive).toBeTruthy();
  });
});
