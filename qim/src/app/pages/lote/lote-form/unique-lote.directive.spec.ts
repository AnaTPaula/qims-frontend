import { UniqueLoteDirective } from './unique-lote.directive';

describe('UniqueLoteDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueLoteDirective();
    expect(directive).toBeTruthy();
  });
});
